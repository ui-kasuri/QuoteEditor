<%# app/views/quotes/create.turbo_stream.erb %>

<%# shortest way %>
<%= turbo_stream.prepend "quotes", @quote %>
<%= turbo_stream.update Quote.new, "" %>
<%# If we're going to stack flash messages and
  have a single-page application effect, we can use prepend.
  If we're going to have a single flash message on
  the screen at the time, we can use replace.
 %>
<%# <%= turbo_stream.prepend "flash", partial: "layouts/flash" %> %>
<%= render_turbo_stream_flash_messages %>

<%# another way way %>

<%#= turbo_stream.prepend "quotes", partial: "quotes/quote", locals: { quote: @quote } %>
<%#= turbo_stream.update Quote.new, "" %>

<%# another way way %>

<%#= turbo_stream.prepend "quotes" do %>
  <%#= render @quote %>
<%# end %>

<%#= turbo_stream.update Quote.new, "" %>

<%# another way%>

<%#= turbo_stream.prepend "quotes" do %>
  <%#= render partial: "quotes/quote", locals: { quote: @quote } %>
<%#end %>
<%#= turbo_stream.update Quote.new, "" %>

